{
  "name": "mcpaystream-backend",
  "version": "1.0.0",
  "description": "MCPayStream Backend - Blockchain-powered payment tracking for content creators",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "devnet:setup": "./setup-devnet.sh",
    "devnet:keypair": "node scripts/generate-devnet-keypair.js",
    "devnet:address": "node -e \"const fs = require('fs'); const path = require('path'); const keypairPath = path.join(require('os').homedir(), '.config', 'solana', 'mcpaystream.json'); try { const data = JSON.parse(fs.readFileSync(keypairPath, 'utf8')); const { Keypair } = require('@solana/web3.js'); const kp = Keypair.fromSecretKey(new Uint8Array(data)); console.log('🔑 Devnet Wallet Address:', kp.publicKey.toString()); } catch (e) { console.log('❌ No keypair found. Run: npm run devnet:keypair'); }\"",
    "devnet:balance": "node -e \"const fs = require('fs'); const path = require('path'); const keypairPath = path.join(require('os').homedir(), '.config', 'solana', 'mcpaystream.json'); try { const data = JSON.parse(fs.readFileSync(keypairPath, 'utf8')); const { Keypair } = require('@solana/web3.js'); const kp = Keypair.fromSecretKey(new Uint8Array(data)); console.log('🔑 Wallet Address:', kp.publicKey.toString()); console.log('💡 Check balance at: https://explorer.solana.com/address/' + kp.publicKey.toString() + '?cluster=devnet'); } catch (e) { console.log('❌ No keypair found. Run: npm run devnet:keypair'); }\""
  },
  "keywords": [
    "solana",
    "blockchain",
    "payments",
    "content-creators",
    "prisma",
    "postgresql",
    "supabase"
  ],
  "author": "MCPayStream Team",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "@solana/web3.js": "^1.91.0",
    "axios": "^1.6.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "node-cron": "^3.0.3",
    "qrcode": "^1.5.3",
    "socket.io": "^4.7.4",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "prisma": "^5.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
